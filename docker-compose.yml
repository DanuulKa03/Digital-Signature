services:
  alica:
    privileged: true
    cap_add:
      - ALL
    ipc: host
    working_dir: /firmware
    build:
      dockerfile: ./Dockerfile
      context: .
    volumes:
      - .:/firmware
      - ./docker/device/root:/root/
  bob:
    cap_add:
      - ALL